#include <stdio.h>
int main()
{ // wrtie a code for percentage of students out of 40 in 4 subs ?
    float x = 39.5;
    float y = 32.7;
    float z = 40.0;
    float a = 33.9;
    float percentage = (x + y + z + a) * 100 / 160;
    printf("your percentage is = %f", percentage);

    return 0;
}